# Core API Service Dependencies
fastapi==0.110.0
uvicorn==0.28.0
pydantic==2.6.4
python-multipart==0.0.9
email-validator==2.1.1

# Database (Optional - App falls back to local JSON database if MongoDB is missing)
pymongo==4.6.2

# AI / ML Analytics (Optional - App falls back to high-fidelity mathematical simulations if missing)
scikit-learn==1.4.1.post1
opencv-python==4.9.0.80
numpy==1.26.4
